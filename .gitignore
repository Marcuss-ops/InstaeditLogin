*~
/build/
# Build output
# Build output directory
# Dependency directories
/dist/
*.dll
# Docker
docker-compose.override.yml
.DS_Store
*.dylib
.env
.env.dev
# Defence-in-depth: any `.env.<something>` filename (dev/local/staging/prod)
# also matches so future contributor `cp .env .env.staging` doesn't leak.
.env.*
# Environment variables (contains secrets!)
*.exe
*.exe~
# Go build artifacts
/seed
go.work
/test-youtube-upload
# IDE
.idea/
*.log
# Logs
# OS files
*.out
*.so
*.swo
*.swp
*.test
Thumbs.db
vendor/
.vercel
.vscode/


# --- restored project-universal (post-cleanup 2026-07-11) ---
*.gen.go
*.pb.go
*.pprof
*.prof
*.so.*
/bin/
go.work.sum
secrets/

# --- restored project-universal Round 2b (post-cleanup 2026-07-11) ---
coverage.html
coverage.out

# Python bytecode
__pycache__/
*.pyc

# Binaries produced by `go build` in repo root
/distribute_channels_to_managers
/oauth-scope-canary
/set_password
/create-user
/worker
/yttest
/batch-import-drive-folder
/published

# Backup / stale files (cleanup 2026-08-02) — never commit editor or
# script-generated backups; restore from git history instead.
*.bak
*.bak2
*.backup
*.old

# Database dumps (may contain real data/credentials)
/backups/
*.dump
.env*
!.env.dev.example
!.env.test.example
!.env.production.example
