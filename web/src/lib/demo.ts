/**
 * Demo mode — minimal gate for the landing page.
 * When `VITE_DEMO_MODE === "true"`, the Login page skips network
 * calls and redirects directly to the landing.
 */
export const DEMO_MODE: boolean = false;
