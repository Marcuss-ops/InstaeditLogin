services:
  minio:
    ports:
      - "127.0.0.1:19000:9000"
    environment:
      MINIO_ROOT_USER: instaedit
      MINIO_ROOT_PASSWORD: change-this-local-secret
  api:
    ports:
      - "127.0.0.1:8080:8080"
networks:
  media-backend:
    internal: true
